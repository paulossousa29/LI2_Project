#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "estado.h"

int main()
{
    //char linha[100];
    //ESTADO e;

    printf("\n>>>>>>>>REVERSI<<<<<<<<\n");
    /*
    while(toupper(linha[0])!='Q')
    {
        menu ();
        fgets(linha, 100, stdin);
        executa_opcao(linha, &e);
    }
    */
    return 0;
}
